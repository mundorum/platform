[project]
name = "noid-runner"
version = "0.1.0"
description = "Shared scene execution logic for the noid platform"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
    "mundorum-noid>=0.1.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
